class DataGrid extends EnigmaticElement{async connectedCallback(){const a=this.getAttribute("data"),b=this.getAttribute("rows"),c=this.getAttribute("cols"),d=this.getAttribute("id");this.style.display="grid",this.style.gridTemplateColumns=`repeat(${c}, 1fr)`,this.style.gridTemplateRows=`repeat(${b}, 1fr)`,this.innerHTML+=`
          <style>
            cell {
                width: 10rem;
                padding: 0.5rem;
            }
          </style>`;let e="";for(let a=0;a<b;a++)for(let b=0;b<c;b++)e+=`<cell id='${d}-c${b}r${a}'>${b}, ${a}</cell>`;this.innerHTML=e}set(){}}customElements.define("data-grid",DataGrid);